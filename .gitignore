# dependencies
node_modules/

# next.js
.next/
out/

# env files (NEVER commit these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# build artifacts
tsconfig.tsbuildinfo

# tooling / local config
.claude/
.agents/
.playwright-mcp/

# supabase local dev (keep migrations, ignore local state)
supabase/.temp/
supabase/.branches/

# openspec (SDD planning artifacts — local only)
openspec/

# OS
.DS_Store
Thumbs.db

# dev screenshots / local debug images (root level)
/*.png
/*.jpg
/*.jpeg
/*.webp

# pnpm (project uses npm)
pnpm-lock.yaml
